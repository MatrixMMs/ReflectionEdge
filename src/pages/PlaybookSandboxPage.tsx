import React from 'react';
import PlaybookSandbox from '../components/playbook/PlaybookSandbox';

const PlaybookSandboxPage: React.FC = () => {
  return (
    <div style={{ width: '100vw', height: '100vh', background: '#18181b', margin: 0, padding: 0 }}>
      <PlaybookSandbox />
    </div>
  );
};

export default PlaybookSandboxPage; 